# Make a folder named "fastfetch" In the ".config" folder in your home directory

# Copy the config.jsonc into the new directory ('fastfetch').

# Go to the ".config/fastfetch" folder and edit "config.jsonc"

# Change "source" section to the path of the file in the "ascii" folder you wish to use(Path to your logo).

# Save and exit

# Close the current terminal session and reopen to see the new display
